from batchq.core import batch

class Model(batch.BatchQ):
    ctrl = batch.Controller(object)
    fnc1 = batch.Function().throw(exception = StandardError("Custom error class"))
    fnc2 = batch.Function().throw("Standard error")

Model().fnc1()
Model().fnc2()
