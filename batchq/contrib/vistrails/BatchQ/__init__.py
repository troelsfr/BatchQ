import sys
##
# TODO list:
# 1) Write the JobInfo module
# 2) Make implement generator functionality
# 3) Try to see what happens when things are connected different from what you are supposed to do
# (V) 4) Extend run job
# 5) Error, when working directory is specified but no input directory. Working directory not used
# 6) check wether it uploads anything

version = '0.2'
#identifier = 'edu.ethz.sci.vistrails.batchq'
identifier = 'org.comp-phys.batchq'
name = 'Job Submission'

#from init import all_modules


#def selfRegister():
#
#  reg = core.modules.module_registry.get_module_registry()
#
#  register_parameters(Model)
#
#  reg.add_module(ClassicalSpinModel,namespace="Models",abstract=True)
#  reg.add_module(ClassicalIsingModel,namespace="Models")
#  reg.add_module(ClassicalXYModel,namespace="Models")
#  reg.add_module(ClassicalHeisenbergModel,namespace="Models")
#
#  register_model(SpinModel)
#  register_model(BosonHubbardModel)
#  register_model(HardcoreBosonModel)
